name: APM-to-DAP-Packing-Workflow
version: 1.0.0
description: Dendritic Agent Package M
author: APM Team

scripts:
  audit: "copilot --log-level all --log-dir copilot-logs --allow-all-tools -p audit-workflow.prompt.md"
  start: "copilot --log-level all --log-dir copilot-logs --allow-all-tools -p hello-world.prompt.md"
  test-all: "copilot --log-level all --log-dir copilot-logs --allow-all-tools -p hello-world.prompt.md"

dependencies:
  apm:
    - danielmeppiel/compliance-rules
  mcp:
    - microsoft/playwright-mcp
